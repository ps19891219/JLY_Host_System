# Preview gate

Do not merge until Preview confirms: DM and NPC both render when their role rows exist; editing NPC leaves DM unchanged; editing DM leaves NPC unchanged; empty batch field operations are blocked.
