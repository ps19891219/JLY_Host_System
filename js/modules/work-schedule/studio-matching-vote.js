(function(){'use strict';const $=id=>document.getElementById(id),id=new URLSearchParams(location.search).get('id');let matching=null;
function esc(v){return String(v||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function load(){if(!id){$('voteSlots').textContent='網址缺少媒合 ID';return}const r=await fetch('/api/studio-matching-vote-context?id='+encodeURIComponent(id)+'&testMode=1',{credentials:'same-origin',cache:'no-store'}),d=await r.json();if(!r.ok||!d.success)throw new Error(d.error||'讀取失敗');matching=d.matching;$('voteTitle').textContent=matching.workName||matching.title||'配合時間';$('voteHost').innerHTML=matching.hostName?'<div class="studio-vote-host">主揪：<strong>'+esc(matching.hostName)+'</strong></div>:'';$('voteSlots').innerHTML=(matching.candidateSlots||[]).map(s=>'<label class="studio-vote-slot"><input type="checkbox" data-slot="'+esc(s.id)+'"><span>'+esc(s.date+' '+s.time)+'</span></label>').join('')||'<div>目前沒有候選時段。</div>'}
async function submit(){const name=$('voteTestName').value.trim();if(!name)return alert('測試期間請輸入姓名。');const slotIds=[...document.querySelectorAll('[data-slot]:checked')].map(x=>x.dataset.slot),b=$('voteSubmit');b.disabled=true;try{const r=await fetch('/api/studio-matching-vote-context',{method:'POST',credentials:'same-origin',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,testMode:'1',testName:name,slotIds})}),d=await r.json();if(!r.ok||!d.success)throw new Error(d.error||'送出失敗');alert('已送出配合時間（測試模式）。')}catch(e){alert('送出失敗：'+e.message)}finally{b.disabled=false}}
$('voteSubmit').onclick=submit;load().catch(e=>$('voteSlots').textContent='讀取失敗：'+(e.message||e));})();