(function(){
'use strict';
const db=window.db||firebase.firestore(),shifts=db.collection('workShifts');
let people=[],month=new Date(),view='all',currentPersonId=null,calMonth=new Date(),selectedDates=new Set(),rolePools=[],activeRoleId=null,assignments={};
const $=id=>document.getElementById(id),pad=n=>String(n).padStart(2,'0'),dateKey=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`,monthKey=d=>`${d.getFullYear()}-${pad(d.getMonth()+1)}`;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const nameOf=p=>p?.displayName||p?.playerName||p?.nickname||'未命名';
function endDate(date,start,end){const d=new Date(`${date}T${end}:00`);if(end<=start)d.setDate(d.getDate()+1);return dateKey(d)}
async function resolveMe(){try{const i=window.JLYIdentity&&await window.JLYIdentity.getCurrentIdentity?.();currentPersonId=i?.personId||i?.playerId||i?.id||null}catch(_){}}
async function loadPeople(){people=await window.JLYMemberPickerData.loadPersonDirectory()}
async function load(){const key=monthKey(month);$('monthLabel').textContent=`${month.getFullYear()} 年 ${month.getMonth()+1} 月`;const snap=await shifts.where('monthKey','==',key).get();let rows=snap.docs.map(d=>({id:d.id,...d.data()})).filter(x=>x.status!=='cancelled');if(view==='mine')rows=rows.filter(x=>(x.personIds||[]).includes(currentPersonId));rows.sort((a,b)=>(a.date+a.startTime).localeCompare(b.date+b.startTime));render(rows)}
function render(rows){if(!rows.length){$('scheduleList').innerHTML='<div class="empty">這個月還沒有班表。</div>';return}const groups={};rows.forEach(x=>(groups[x.date]??=[]).push(x));$('scheduleList').innerHTML=Object.entries(groups).map(([date,list])=>`<article class="day-card"><h2>${esc(date)}</h2>${list.map(x=>{const missing=Math.max(0,(x.requiredCount||0)-(x.personIds||[]).length);return `<div class="shift-card"><div class="shift-title">${esc(x.workName)} · ${esc(x.roleName)}</div><div class="shift-meta">${esc(x.startTime)}–${x.endDate!==x.date?'翌日 ':''}${esc(x.endTime)}${x.studioName?' · '+esc(x.studioName):''}</div><div class="people">${(x.people||[]).map(p=>`<span class="person-chip">${esc(p.name)}</span>`).join('')}</div>${missing?`<span class="pending-badge">待處理 · ${esc(x.roleName)} 尚缺 ${missing} 人</span>`:''}${x.note?`<div class="shift-meta">${esc(x.note)}</div>`:''}<div class="shift-actions"><button type="button" data-calendar="${esc(x.id)}">📅 加入行事曆</button></div></div>`}).join('')}</article>`).join('');document.querySelectorAll('[data-calendar]').forEach(b=>b.onclick=()=>calendar(b.dataset.calendar))}
function newRole(name='DM'){return {id:`r${Date.now()}${Math.random().toString(36).slice(2,6)}`,name,personIds:new Set()}}
function openNew(){$('shiftForm').reset();$('startTime').value='11:00';$('endTime').value='18:00';$('requiredCount').value='1';selectedDates=new Set();assignments={};calMonth=new Date(month.getFullYear(),month.getMonth(),1);rolePools=[newRole('DM'),newRole('NPC')];activeRoleId=rolePools[0].id;renderRoles();renderCalendar();renderDaily();$('shiftDialog').showModal()}
function renderRoles(){const host=$('rolePools');host.innerHTML=rolePools.map(r=>`<div class="role-pool" data-role-pool="${r.id}"><div class="role-head"><input data-role-name="${r.id}" value="${esc(r.name)}" placeholder="角色名稱"><button type="button" data-remove-role="${r.id}">刪除</button></div><input data-person-search="${r.id}" placeholder="搜尋人員加入 ${esc(r.name)}"><div class="role-people">${[...r.personIds].map(id=>`<button type="button" class="pool-person selected" data-pool-person="${r.id}|${id}">${esc(nameOf(people.find(p=>p.id===id)))} ×</button>`).join('')}</div><div class="person-search-results" data-search-results="${r.id}"></div></div>`).join('');
host.querySelectorAll('[data-role-name]').forEach(i=>i.oninput=()=>{const r=rolePools.find(x=>x.id===i.dataset.roleName);r.name=i.value;renderRoleSelector()});host.querySelectorAll('[data-remove-role]').forEach(b=>b.onclick=()=>{if(rolePools.length===1)return;rolePools=rolePools.filter(r=>r.id!==b.dataset.removeRole);if(activeRoleId===b.dataset.removeRole)activeRoleId=rolePools[0]?.id;renderRoles();renderDaily()});host.querySelectorAll('[data-pool-person]').forEach(b=>b.onclick=()=>{const [rid,pid]=b.dataset.poolPerson.split('|');rolePools.find(r=>r.id===rid)?.personIds.delete(pid);renderRoles();renderDaily()});host.querySelectorAll('[data-person-search]').forEach(i=>i.oninput=()=>renderSearch(i.dataset.personSearch,i.value));renderRoleSelector()}
function renderSearch(rid,q){const box=document.querySelector(`[data-search-results="${rid}"]`),role=rolePools.find(r=>r.id===rid);if(!q.trim()){box.innerHTML='';return}const found=window.JLYMemberPickerData.searchMembers(people,q).slice(0,20);box.innerHTML=found.map(p=>`<button type="button" class="search-row" data-add-person="${p.id}"><span>${esc(nameOf(p))}</span><small>${esc(window.JLYMemberPickerData.getIdentityLabel?.(p)||'既有人員')}</small></button>`).join('')||'<div class="empty-mini">找不到人員</div>';box.querySelectorAll('[data-add-person]').forEach(b=>b.onclick=()=>{role.personIds.add(b.dataset.addPerson);renderRoles();renderDaily()})}
function renderRoleSelector(){$('roleSelector').innerHTML=rolePools.map(r=>`<button type="button" class="role-choice ${r.id===activeRoleId?'selected':''}" data-role-choice="${r.id}">${esc(r.name||'未命名角色')}</button>`).join('');document.querySelectorAll('[data-role-choice]').forEach(b=>b.onclick=()=>{activeRoleId=b.dataset.roleChoice;renderRoleSelector();renderDaily()})}
function renderCalendar(){const y=calMonth.getFullYear(),m=calMonth.getMonth(),first=new Date(y,m,1),days=new Date(y,m+1,0).getDate();$('calLabel').textContent=`${y} 年 ${m+1} 月`;let html='';for(let i=0;i<first.getDay();i++)html+='<button class="date-btn blank" type="button"></button>';for(let d=1;d<=days;d++){const key=dateKey(new Date(y,m,d));html+=`<button type="button" class="date-btn ${selectedDates.has(key)?'selected':''}" data-date="${key}">${d}</button>`}$('dateCalendar').innerHTML=html;$('dateCount').textContent=`已選 ${selectedDates.size} 天`;document.querySelectorAll('[data-date]').forEach(b=>b.onclick=()=>{const k=b.dataset.date;selectedDates.has(k)?selectedDates.delete(k):selectedDates.add(k);renderCalendar();renderDaily()})}
function renderDaily(){const role=rolePools.find(r=>r.id===activeRoleId),dates=[...selectedDates].sort(),required=Math.max(0,Number($('requiredCount').value)||0);if(!role||!dates.length){$('dailyAssignments').innerHTML='<div class="empty-mini">請先選日期與角色。</div>';return}$('dailyAssignments').innerHTML=dates.map(date=>{const key=`${date}|${role.id}`,set=assignments[key]||new Set(),missing=Math.max(0,required-set.size);return `<div class="daily-row"><div class="daily-head"><strong>${date}</strong><span class="daily-status ${missing?'pending':''}">${missing?`待處理 · 尚缺 ${missing} 人`:`已完成 ${set.size}/${required}`}</span></div><div class="assign-people">${[...role.personIds].map(id=>`<button type="button" class="assign-person ${set.has(id)?'selected':''}" data-assign="${date}|${role.id}|${id}">${esc(nameOf(people.find(p=>p.id===id)))}</button>`).join('')||'<span class="empty-mini">請先在上方把人員加入這個角色。</span>'}</div></div>`}).join('');document.querySelectorAll('[data-assign]').forEach(b=>b.onclick=()=>{const [date,rid,pid]=b.dataset.assign.split('|'),key=`${date}|${rid}`;assignments[key]??=new Set();assignments[key].has(pid)?assignments[key].delete(pid):assignments[key].add(pid);renderDaily()})}
async function save(e){e.preventDefault();const role=rolePools.find(r=>r.id===activeRoleId),dates=[...selectedDates].sort();if(!role||!role.name.trim()||!dates.length)return alert('請先選擇日期與角色。');const required=Math.max(0,Number($('requiredCount').value)||0),batch=db.batch();dates.forEach(date=>{const ids=[...(assignments[`${date}|${role.id}`]||new Set())],persons=ids.map(id=>({personId:id,name:nameOf(people.find(p=>p.id===id))}));batch.set(shifts.doc(),{workName:$('workName').value.trim(),studioName:$('studioName').value.trim(),roleName:role.name.trim(),rolePoolId:role.id,startTime:$('startTime').value,endTime:$('endTime').value,date,endDate:endDate(date,$('startTime').value,$('endTime').value),monthKey:date.slice(0,7),requiredCount:required,staffingStatus:ids.length<required?'pending':'complete',missingCount:Math.max(0,required-ids.length),personIds:ids,people:persons,note:$('shiftNote').value.trim(),status:'scheduled',schemaVersion:2,source:'work_schedule_v1',scriptId:null,studioId:null,branchId:null,activityId:null,createdAt:firebase.firestore.FieldValue.serverTimestamp(),updatedAt:firebase.firestore.FieldValue.serverTimestamp()})});await batch.commit();$('shiftDialog').close();await load()}
async function calendar(id){const doc=await shifts.doc(id).get();if(!doc.exists)return;const x=doc.data(),start=`${x.date.replaceAll('-','')}T${x.startTime.replace(':','')}00`,ed=x.endDate||x.date,end=`${ed.replaceAll('-','')}T${x.endTime.replace(':','')}00`,title=encodeURIComponent(`${x.workName}｜${x.roleName}`),details=encodeURIComponent((x.people||[]).map(p=>p.name).join('、')+(x.note?`\n${x.note}`:'')),location=encodeURIComponent(x.studioName||'');window.open(`https://calendar.google.com/calendar/render?action=TEMPLATE&text=${title}&dates=${start}/${end}&details=${details}&location=${location}`,'_blank','noopener')}
$('openCreate').onclick=openNew;$('closeDialog').onclick=()=>$('shiftDialog').close();$('shiftForm').onsubmit=save;$('addRole').onclick=()=>{rolePools.push(newRole(''));renderRoles()};$('calPrev').onclick=()=>{calMonth=new Date(calMonth.getFullYear(),calMonth.getMonth()-1,1);renderCalendar()};$('calNext').onclick=()=>{calMonth=new Date(calMonth.getFullYear(),calMonth.getMonth()+1,1);renderCalendar()};$('requiredCount').oninput=renderDaily;$('prevMonth').onclick=()=>{month=new Date(month.getFullYear(),month.getMonth()-1,1);load()};$('nextMonth').onclick=()=>{month=new Date(month.getFullYear(),month.getMonth()+1,1);load()};document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>{view=b.dataset.view;document.querySelectorAll('[data-view]').forEach(x=>x.classList.toggle('active',x===b));load()});
(async()=>{await Promise.all([resolveMe(),loadPeople()]);await load()})().catch(err=>{$('scheduleList').innerHTML=`<div class="empty">班表讀取失敗：${esc(err.message)}</div>`;console.error(err)});
})();