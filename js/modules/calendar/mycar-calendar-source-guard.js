(function () {
  "use strict";

  /*
    MyCar Google Calendar source validation is now owned by
    mycar-calendar-repair.js as a single flow.

    Keep this legacy file inert because calendar-data.js may still load it
    on MyCar. Do not install a second button wrapper or OAuth flow here.
  */
})();
