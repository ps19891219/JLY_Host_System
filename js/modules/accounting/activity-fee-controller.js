(function(){
  "use strict";
  const money=value=>`$${Number(value||0).toLocaleString("zh-TW")}`,escape=value=>String(value==null?"":value).replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/"/g,"&quot;");
  const options=members=>members.map(item=>`<option value="${escape(item.personId)}">${escape(item.displayName)}</option>`).join("");
  async function mount(context){
    const {section,carId,members,currentPersonId,isManager}=context,data=window.JLYActivityFeeData,repository=window.JLYActivityFeeRepository,players=members.filter(member=>(member.roles||[]).includes("player"));
    if(!section||!data||!repository)return;const old=section.querySelector("#activityFeeSection");if(old)old.remove();
    const loaded=await repository.load(carId);if(loaded.plan){const synced=data.syncPlayers(loaded.plan,players.map(item=>item.personId),currentPersonId,new Date().toISOString());if(synced!==loaded.plan){loaded.plan=synced;if(isManager)await repository.savePlan(carId,synced,currentPersonId);}}const summary=data.summarize(loaded.plan,loaded.memberPayments,loaded.vendorPayments),box=document.createElement("section");box.id="activityFeeSection";box.className="accounting-fee-section";
    if(!loaded.plan){
      box.innerHTML=`<div class="accounting-list-heading"><h4>🎭 劇本費與店家核銷</h4><small>玩家區 ${players.length} 人</small></div>${isManager?`<form id="feePlanForm" class="accounting-quick-form"><label>店家／工作室<input name="vendorName" required></label><label>店家基本費用<input name="vendorBaseAmount" type="number" min="1" required></label><label>每位玩家劇本費<input name="playerFee" type="number" min="1" required></label><p>玩家應收將依玩家區 ${players.length} 人自動計算。</p><button type="submit">建立劇本費</button></form>`:`<p class="empty-text">尚未建立劇本費資料</p>`}`;
      section.appendChild(box);const form=box.querySelector("#feePlanForm");if(form)form.addEventListener("submit",async event=>{event.preventDefault();const values=Object.fromEntries(new FormData(form)),plan=data.buildPlan({...values,carId,playerIds:players.map(item=>item.personId)},currentPersonId,new Date().toISOString());await repository.savePlan(carId,plan,currentPersonId);mount(context);});return;
    }
    const memberRows=summary.members.map(item=>`<li><span>${escape((members.find(member=>member.personId===item.personId)||{}).displayName||"成員")}</span><b>${money(item.paid)} / ${money(item.amount)}</b></li>`).join(""),feeRows=(loaded.plan.feeItems||[]).map(item=>`<li><span>${escape(item.title)}<small>${escape(item.allocationType)}</small></span><b>＋${money(item.amount)}</b></li>`).join("")||`<li><span>目前沒有額外費用</span></li>`,customRows=players.map(item=>`<label>${escape(item.displayName)}<input name="custom_${escape(item.personId)}" type="number" min="0" value="0"></label>`).join("");
    box.innerHTML=`<div class="accounting-list-heading"><h4>🎭 劇本費與店家核銷</h4><small>${escape(loaded.plan.vendor.displayName)}｜玩家區 ${players.length} 人</small></div><div class="accounting-fee-summary"><span>玩家待收 <b>${money(summary.memberOutstanding)}</b></span><span>店家待付 <b>${money(summary.vendorOutstanding)}</b></span><span>車團暫存 <b>${money(summary.custodyBalance)}</b></span></div><h5>費用項目</h5><ul class="accounting-fee-members"><li><span>基本劇本費<small>${players.length} 人 × ${money(loaded.plan.playerFee)}</small></span><b>${money(loaded.plan.vendorBaseAmount)}</b></li>${feeRows}</ul><h5>玩家繳費</h5><ul class="accounting-fee-members">${memberRows}</ul>${isManager?`<div class="accounting-fee-forms"><form id="feeItemForm" class="accounting-quick-form"><h5>＋ 新增費用項目</h5><input name="title" placeholder="例如：指定 DM 費" required><select name="category"><option value="designated_dm">指定 DM 費</option><option value="other">其他費用</option></select><input name="amount" type="number" min="1" placeholder="金額" required><select name="allocationType" id="feeAllocationType"><option value="equal">玩家均分</option><option value="specific">指定玩家支付</option><option value="host">主揪支付</option><option value="custom">自訂分攤</option></select><label id="feeSpecificPerson" hidden>負擔玩家<select name="personId">${options(players)}</select></label><div id="feeCustomShares" class="accounting-custom-shares" hidden>${customRows}</div><input name="note" placeholder="備註，例如：指定小霙 DM"><button type="submit">新增費用</button></form><form id="memberFeeForm" class="accounting-quick-form"><h5>登記玩家繳費</h5><select name="personId">${options(players)}</select><select name="kind"><option value="payment">收款／玩家訂金</option><option value="refund">退款</option></select><input name="amount" type="number" min="1" placeholder="金額" required><button type="submit">登記</button></form><form id="vendorFeeForm" class="accounting-quick-form"><h5>登記店家款項</h5><select name="kind"><option value="deposit">訂金</option><option value="balance">尾款</option><option value="refund">店家退款</option></select><input name="amount" type="number" min="1" placeholder="金額" required><button type="submit">登記</button></form></div>`:""}`;section.appendChild(box);
    const mode=box.querySelector("#feeAllocationType"),specific=box.querySelector("#feeSpecificPerson"),custom=box.querySelector("#feeCustomShares");if(mode)mode.addEventListener("change",()=>{specific.hidden=mode.value!=="specific";custom.hidden=mode.value!=="custom";});
    const bind=(id,handler)=>{const form=box.querySelector(id);if(form)form.addEventListener("submit",async event=>{event.preventDefault();const values=Object.fromEntries(new FormData(form));await handler(values,form);mount(context);});};
    bind("#feeItemForm",async(values,form)=>{const allocations=players.map(item=>({personId:item.personId,amount:form.elements[`custom_${item.personId}`].value}));const plan=data.addFeeItem(loaded.plan,{...values,allocations},currentPersonId,new Date().toISOString());await repository.savePlan(carId,plan,currentPersonId);});
    bind("#memberFeeForm",values=>repository.recordMemberPayment(carId,values,currentPersonId));bind("#vendorFeeForm",values=>repository.recordVendorPayment(carId,{...values,externalPartyId:loaded.plan.vendor.externalPartyId},currentPersonId));
  }
  window.JLYActivityFeeController={mount};
})();
