"use strict";
const bookingRepo=require("../services/firebase/studio-booking-repository");const activityService=require("../services/studio/studio-booking-activity-service");const txt=v=>String(v==null?"":v).trim();
function send(res,code,body){res.statusCode=code;res.setHeader("Content-Type","application/json; charset=utf-8");res.setHeader("Cache-Control","private, no-store");res.end(JSON.stringify(body))}
function body(req){if(req&&req.body&&typeof req.body==="object")return req.body;try{return JSON.parse(String(req&&req.body||"{}"))}catch{return{}}}
module.exports=async function(req,res){if(!req||req.method!=="POST")return send(res,405,{success:false,error:"method_not_allowed"});const b=body(req),id=txt(b.bookingId),action=txt(b.action);if(!id)return send(res,400,{success:false,error:"booking_id_required"});try{if(action==="accept"){const result=await activityService.confirmBooking(id,{confirmedBy:b.actorPersonId});return send(res,result.confirmed?200:409,{success:result.confirmed,result})}if(action==="reject"){const result=await bookingRepo.updateStatus(id,"rejected",{resolvedBy:txt(b.actorPersonId)||null,resolvedAt:new Date().toISOString()});return send(res,200,{success:true,result})}if(action==="propose_adjustment"){const result=await bookingRepo.updateStatus(id,"adjustment_proposed",{proposedDate:txt(b.date)||null,proposedTime:txt(b.time)||null,proposalNote:txt(b.note)||null});return send(res,200,{success:true,result})}return send(res,400,{success:false,error:"booking_action_invalid"})}catch(e){console.error("studio booking action failed",e);return send(res,500,{success:false,error:String(e&&e.message||"booking_action_failed")})}};