"use strict";const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");test("LINE group links and UI enforce existing-name selection",()=>{const card=fs.readFileSync(path.join(__dirname,"../services/line/member-welcome-card.js"),"utf8");const actions=fs.readFileSync(path.join(__dirname,"../js/car/car-view-actions.js"),"utf8");assert.match(card,/entry=player&source=line_group/);assert.match(card,/entry=dm&source=line_group/);assert.match(actions,/playerClaimablePeople/);assert.match(actions,/dmClaimableSlots/);assert.match(actions,/請聯絡主揪新增/);assert.doesNotMatch(actions,/名單沒有我／新增我的名字/);});