const fs=require('fs'),assert=require('assert');const s=fs.readFileSync('js/modules/car/detail/player/player-actions.js','utf8');const remove=s.slice(s.indexOf('async function removePlayerFromCar'));assert.match(remove,/await syncKnownMembershipMutation/);assert.match(remove,/await syncStudioRecruitmentMutation/);assert.ok(remove.indexOf('syncKnownMembershipMutation')<remove.indexOf('syncStudioRecruitmentMutation'));assert.match(remove,/publicRecruitment/);console.log('player removal reconnects Studio recruitment projection');