const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');test('Pending Actions use prepared inbox docs and no collection scan',()=>{const s=fs.readFileSync('services/firebase/pending-action-repository.js','utf8');assert.match(s,/pendingActionPersonViews/);assert.match(s,/pendingActionStudioViews/);assert.match(s,/runTransaction/);assert.doesNotMatch(s,/collection\(COLLECTION\)\.get\(/);assert.doesNotMatch(s,/onSnapshot/)});