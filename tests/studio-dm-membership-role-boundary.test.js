const fs=require('fs'),assert=require('assert');const s=fs.readFileSync('services/studio/studio-activity-membership-service.js','utf8');assert.match(s,/roleChoice/);assert.match(s,/personNameChanged:false/);assert.doesNotMatch(s,/collection\(["']players["']\)/);assert.doesNotMatch(s,/collection\(["']cars["']\)\.get/);assert.doesNotMatch(s,/onSnapshot/);console.log('dm activity membership role boundary ok');