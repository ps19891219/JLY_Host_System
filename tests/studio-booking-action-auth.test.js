const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');test('Studio booking API derives actor from signed member session',()=>{const s=fs.readFileSync('api/studio-booking-action.js','utf8');assert.match(s,/verifyMemberSession\(readCookie\(req\)\)/);assert.match(s,/studioAllows/);assert.doesNotMatch(s,/confirmedBy:b\.actorPersonId/);assert.doesNotMatch(s,/resolvedBy:txt\(b\.actorPersonId/);assert.match(s,/studio_membership_required/)});