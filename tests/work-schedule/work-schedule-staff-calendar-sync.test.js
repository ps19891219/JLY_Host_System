import fs from 'node:fs';import assert from 'node:assert';const p='js/modules/work-schedule/work-schedule-staff-page.js';const s=fs.readFileSync(p,'utf8');assert(s.includes("function nextDate(date)"),'cross-day date helper missing');assert(s.includes("r.endTime<=r.startTime"),'overnight detection missing');assert(s.includes("await findExisting(token,g,id)"),'Google Calendar lookup-before-write missing');assert(s.includes("method:'PATCH'"),'existing event update missing');assert(s.includes("sourceModule:'work-schedule-staff'"),'JLY staff event marker missing');console.log('work-schedule-staff-calendar-sync regression checks passed');