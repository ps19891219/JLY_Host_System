const fs=require('fs'),assert=require('assert');const h=fs.readFileSync('pages/studio-matching.html','utf8'),j=fs.readFileSync('js/modules/work-schedule/studio-matching-workspace.js','utf8');assert(h.includes('studioMatchingWork'));assert(h.includes('studioMatchingCalendar'));assert(j.includes("label:'早'")&&j.includes("label:'中'")&&j.includes("label:'晚'"));assert(j.includes('selectedDates=new Set()'));assert(j.includes('candidateSlots()'));assert(j.includes('workId,workName'));assert(!j.includes("slots=[{date:''"));assert(!j.includes("collection('workShifts')"));console.log('studio matching calendar presets ok');