const fs=require('fs'),assert=require('assert');const s=fs.readFileSync('services/studio/studio-booking-activity-service.js','utf8');assert.match(s,/bookingAcceptancePlan/);assert.match(s,/existing_activity_required/);assert.match(s,/needs_human_confirmation/);assert.match(s,/collection\(["']cars["']\)\.doc\(\)/);assert.doesNotMatch(s,/collection\(["']cars["']\)\.get\(/);assert.doesNotMatch(s,/onSnapshot/);console.log('studio booking confirm one-activity boundary ok');