"use strict";const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");test("Person Directory batch contract is documented",()=>{const s=fs.readFileSync(path.join(__dirname,"../docs/PERSON_DIRECTORY_V1.md"),"utf8");assert.match(s,/no second Guest or Member database/i);assert.match(s,/Public car entry does not display/);});