const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');test('EditCar public/private persists explicit recruitment preference',()=>{const s=fs.readFileSync('js/editcar.js','utf8');assert.match(s,/publicRecruitmentPreference:\s*selectedVisibility === "public"/);assert.match(s,/applyRecruitmentMutation/)});test('Studio cancellation projects cancelled same Activity out of recruitment',()=>{const s=fs.readFileSync('services/studio/studio-activity-lifecycle-service.js','utf8');assert.match(s,/before=\{id:snap\.id,\.\.\.snap\.data\(\)\}/);assert.match(s,/afterActivity=\{\.\.\.before,status:"cancelled"/);assert.match(s,/await recruitment\.syncActivity\(afterActivity\)/);assert.doesNotMatch(s,/\.delete\(/)});