"use strict";const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");test("public car entry does not render host roster picker",()=>{const s=fs.readFileSync(path.join(__dirname,"../js/car/car-view-actions.js"),"utf8");const group=s.indexOf("if(isGroupEntry())");const publicNote=s.indexOf("公開揪團報名不顯示主揪後台人員名單");assert.ok(group>=0);assert.ok(publicNote>group);assert.match(s,/targetPlayerId:""/);});