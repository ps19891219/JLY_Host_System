"use strict";const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");test("same-name manual add requires explicit override and keeps separate Person ID",()=>{const s=fs.readFileSync(path.join(__dirname,"../js/modules/car/detail/player/player-search.js"),"utf8");assert.match(s,/輸入 0 代表這是另一位不同的真人/);assert.match(s,/sameNameReferenceIds/);assert.match(s,/collection\("players"\)\.doc\(\)/);});