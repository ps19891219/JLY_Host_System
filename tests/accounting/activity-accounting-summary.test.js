"use strict";
const test=require("node:test"),assert=require("node:assert/strict"),{buildActivityAccountingSummary}=require("../../services/accounting/activity-accounting-summary");
test("車團總帳保留實際付款並產生一對一互抵結果",()=>{const summary=buildActivityAccountingSummary([{type:"expense",amount:690,status:"active",paidBy:"shijie",splitStatus:"completed",splits:[{personId:"shijie",amount:345},{personId:"xiaoying",amount:345,settlementStatus:"payment_due"}]},{type:"expense",amount:100,status:"active",paidBy:"xiaoying",splitStatus:"completed",splits:[{personId:"shijie",amount:40,settlementStatus:"payment_due"},{personId:"xiaoying",amount:60}]}],[]);assert.equal(summary.totalExpense,790);assert.deepEqual(summary.settlementTransfers,[{fromPersonId:"xiaoying",toPersonId:"shijie",amount:305}]);assert.equal(summary.outstandingAmount,305);assert.equal(summary.memberSummaries.find(item=>item.personId==="shijie").paidAmount,690);});
test("已確認核銷只清除未結清金額不刪除車團總支出",()=>{const summary=buildActivityAccountingSummary([{type:"expense",amount:300,status:"active",paidBy:"a",splitStatus:"completed",splits:[{personId:"b",amount:300,settlementStatus:"payment_due"}]}],[{status:"settled",fromPersonId:"b",toPersonId:"a",amount:300}]);assert.equal(summary.totalExpense,300);assert.equal(summary.outstandingAmount,0);assert.deepEqual(summary.settlementTransfers,[]);});
