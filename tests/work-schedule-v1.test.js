const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');
const page=fs.readFileSync('pages/work-schedule.html','utf8'),js=fs.readFileSync('js/modules/work-schedule/work-schedule-v2.js','utf8'),google=fs.readFileSync('js/modules/work-schedule/work-schedule-google.js','utf8'),css=fs.readFileSync('css/pages/work-schedule.css','utf8'),home=fs.readFileSync('index.html','utf8');
test('work schedule keeps independent mobile entry',()=>{assert.match(home,/工作班表/);assert.match(page,/全部班表/);assert.match(page,/只看我的/);assert.match(css,/100dvh/)});
test('work role pools are persistent and fully editable',()=>{assert.match(js,/workScheduleWorks/);assert.match(js,/eligiblePersonIds/);assert.match(js,/persistWork/);assert.match(js,/data-remove-role/);assert.match(js,/data-role-name/)});
test('editing restores full eligible pool but keeps assignment separate',()=>{assert.match(js,/work\?\.roles/);assert.match(js,/eligiblePersonIds:new Set/);assert.match(js,/assignments=\{/);assert.match(js,/row\.personIds/);assert.match(page,/候選人都會保留/)});
test('roles and eligible people can be added or removed while editing',()=>{assert.match(js,/roles\.push\(r\)/);assert.match(js,/roles=roles\.filter/);assert.match(js,/eligiblePersonIds\.add/);assert.match(js,/eligiblePersonIds\.delete/)});
test('person directory remains canonical and guest creation is explicit',()=>{assert.match(js,/loadPersonDirectory/);assert.match(js,/searchMembers/);assert.match(js,/collection\('players'\)\.add/);assert.doesNotMatch(js,/collection\(['"]actors/)});
test('date time work location staffing and note are editable',()=>{for(const id of ['workName','studioName','startTime','endTime','requiredCount','shiftNote'])assert.match(page,new RegExp(`id="${id}"`));assert.match(js,/openExisting/);assert.match(js,/selectedDates=new Set\(\[row\.date\]\)/)});
test('daily assignments are independent from role eligibility',()=>{assert.match(js,/assignments\[key\]/);assert.match(js,/r\.eligiblePersonIds/);assert.match(js,/personIds:ids/)});
test('Google Calendar is the schedule sync destination',()=>{assert.match(page,/自動同步至 Google Calendar/);assert.match(page,/calendar-provider-google/);assert.match(js,/JLYWorkScheduleGoogle\.sync/);assert.match(google,/provider\.updateEvent/);assert.match(google,/provider\.createEvent/);assert.match(google,/eventId/);assert.doesNotMatch(js,/BEGIN:VCALENDAR|Blob\(/)});
test('Google auto update defaults on and survives edits',()=>{assert.match(page,/calendarSyncEnabled[^>]*checkbox[^>]*checked/);assert.match(js,/autoUpdate:calendarEnabled/);assert.match(google,/row\.calendar\?\.eventId/)});
test('cross midnight is retained',()=>{assert.match(js,/end<=start/);assert.match(google,/endDate/)});