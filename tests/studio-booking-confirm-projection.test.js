const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');test('confirmed Booking updates bounded Studio recruitment projection',()=>{const s=fs.readFileSync('services/studio/studio-booking-activity-service.js','utf8');assert.match(s,/await recruitment\.syncActivity\(activity\)/);assert.match(s,/existing_activity_required/);assert.doesNotMatch(s,/collection\("cars"\)\.get\(/);assert.doesNotMatch(s,/onSnapshot/)});