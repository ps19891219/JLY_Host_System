# MyCar focused tests

Run the View-first contract checks with:

`node tests/mycar/mycar-view-first-runner.test.js`

These checks protect the normal-page contract only. They do not perform Firestore writes or recovery.
