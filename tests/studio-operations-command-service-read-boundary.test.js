const assert=require('assert');const s= require('fs').readFileSync('services/studio/studio-operations-command-service.js','utf8');assert.doesNotMatch(s,/\.collection\s*\(|\.where\s*\(|onSnapshot/);assert.match(s,/bookingService\.confirmBooking/);assert.match(s,/bookingRepo\.updateStatus/);assert.match(s,/pendingRepo\.resolve/);assert.match(s,/host_request_adjustment_requires_domain_proposal/);console.log('studio command delegation boundary ok');