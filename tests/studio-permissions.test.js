const assert=require('assert'),P=require('../js/studio/studio-permissions.js');const dm={personId:'p1',roles:['dm'],status:'active'};assert.equal(P.canViewActivity(dm),true);assert.equal(P.canManageBooking(dm),false);assert.equal(P.canEditMembershipRole(dm,{dmPersonId:'p1'}),true);assert.equal(P.canEditMembershipRole(dm,{dmPersonId:'p2'}),false);const mgr={personId:'m1',roles:['manager'],status:'active'};assert.equal(P.canManageBooking(mgr),true);assert.equal(P.canProposeActivityChange(mgr),true);assert.equal(P.canViewActivity({personId:'x',roles:[],permissions:['activity:view'],status:'active'}),true);console.log('studio permissions view/edit separation ok');