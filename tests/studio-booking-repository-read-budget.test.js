const fs=require('fs'),assert=require('assert');const s=fs.readFileSync('services/firebase/studio-booking-repository.js','utf8');assert.match(s,/studioBookingInboxViews/);assert.match(s,/runTransaction/);assert.doesNotMatch(s,/collection\(COLLECTION\)\.get/);assert.doesNotMatch(s,/onSnapshot/);assert.doesNotMatch(s,/collection\(["']cars["']\)\.get/);console.log('studio booking repository read budget ok');