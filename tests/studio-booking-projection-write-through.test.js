const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs');test('Booking confirm keeps one Activity and incrementally projects recruitment',()=>{const s=fs.readFileSync('services/studio/studio-booking-activity-service.js','utf8');assert.match(s,/if\(txt\(booking\.activityId\)\)/);assert.match(s,/if\(plan\.createActivity\)/);assert.match(s,/await recruitment\.syncActivity\(activity\)/);assert.doesNotMatch(s,/collection\("cars"\)\.get\(/);assert.doesNotMatch(s,/onSnapshot/)});