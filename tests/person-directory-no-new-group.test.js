"use strict";const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");test("group entry never submits blank target identity",()=>{const s=fs.readFileSync(path.join(__dirname,"../js/car/car-view-actions.js"),"utf8");assert.match(s,/if\(isGroupEntry\(\)\)/);assert.match(s,/targetPlayerId:select\.value/);assert.match(s,/targetStaffId:select\.value/);});