"use strict";const test=require("node:test");const assert=require("node:assert/strict");const fs=require("node:fs");const path=require("node:path");test("member guest classification derives from identity instead of separate store",()=>{const s=fs.readFileSync(path.join(__dirname,"../js/modules/member/person-directory.js"),"utf8");assert.match(s,/hasFormalIdentity\(p\)\|\|data\(\)\.isLineLinked\(p\)/);assert.doesNotMatch(s,/collection\("members"\)/);assert.doesNotMatch(s,/collection\("guests"\)/);});